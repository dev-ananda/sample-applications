package dev.ananda.kafka.consumer;

/*
@Slf4j
//@Service
public class TestConsumer {

	//@KafkaListener(topics = "sample-test-001")
	public void consume( byte [] newMessage ) throws InvalidProtocolBufferException {
		//Transaction.TransactionProto newRequest = Transaction.TransactionProto.parseFrom( newMessage );
		//log.info( "new message received ==>> {}", new Gson().toJson( newRequest )  );
		log.error( "=======================================================================================================" );
		log.error( "new message received >>>>>>>>>>>>>>>>> " + new String( newMessage ) );
		log.error( "=======================================================================================================" );
	}

}
*/
